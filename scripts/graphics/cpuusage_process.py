import numpy as np
import logging as log

def cpuusage_process():
    serie_name_list = []
    timestamp_mat = np.mat()
    value_mat = np.mat()
    log.debug("cpu_process : ")
    log.debug(str(timestamp_mat))

    return serie_name_list, timestamp_mat, value_mat